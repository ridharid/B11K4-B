<?php>

function cetak_gambar($ukuran) {
        $char1 = "*";
        $char2 = "=";
        $ukuran = 9;
        $onehalf = floor($ukuran/2);
        $onehalf_var = $onehalf
        
        //header
        console.log(" ".repeat($ukuran / 2) + "--- panjang ---" + " ".repeat($ukuran / 2));

        for($baris=0; $baris<=$ukuran) {
            if($ukuran % 2 == 0) {
                echo "*"
            }
        }
}
?>